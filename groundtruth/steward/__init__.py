"""Repository Steward — advisory layer that audits the repository the way
meta/ audits evaluation registers. Declarations in, deterministic findings
out, zero mutation. Imports stdlib only; imported only by cli.py (RC5)."""
