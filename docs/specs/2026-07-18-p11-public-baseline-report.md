# Phase 11 Report — Public Baseline Capture

**Date:** 2026-07-18
**Repository:** https://github.com/Sanjaayyy7/GroundTruth (public, created 2026-07-18 19:49 UTC)
**Baseline state:** main @ 30b95a3 (63 commits), pushed 2026-07-18 ~20:18 UTC over the auto-init stub
**Mode:** Verification. Measure-only — this report is the first repository
change after baseline capture; nothing was modified during measurement.
**Frozen:** this document is the historical comparison point for all future
public-presentation improvements.

## 1. First cloud CI run (the headline measurement)

Run 29659448333, triggered by the initial push, **completed green on
ubuntu-latest in ~17 seconds** (startedAt → updatedAt). All eleven functional
steps succeeded on the first-ever execution outside the development machine:

install → 170 tests → hardened-agent demo → detector quality → two safety
regression gates → evidence audit → second-consumer audit (MiniJudge) →
steward RC1–RC8 → **committed-evidence byte-freshness diff**.

The last step is the significant one: the steward manifest and report,
generated on macOS/arm64 and committed, **regenerated byte-identically on
Linux/x86_64**. Cross-platform byte-determinism of the derived evidence
artifacts was a design claim until this run; it is now a measurement.

## 2. Public clone reproduction

From the public URL on a clean scratch environment:

| Step | Result |
|---|---|
| `git clone` (network) | 0.7 s, `.git` = 516 KB |
| venv + `pip install -e ".[dev]"` | clean |
| `groundtruth run --agent hardened_agent` | robustness 1.00 — matches README |
| `pytest -q` | **170 passed** |

The full Phase 10 reproduction loop now closes from the public internet, not
just the local filesystem.

## 3. Landing experience (desktop, measured via rendered page)

- Green CI check displayed beside HEAD commit — a trust artifact above the fold.
- "63 Commits · 1 Branch · **0 Tags**" — release absence is prominent.
- Directory rows carry honest development dates ("4 days ago", "last week") —
  the natural-evolution signal is real and visible.
- About panel shows the description but **renders markdown literally**:
  "*explains why*" appears with raw asterisks (GitHub descriptions are plain
  text). Finding F-P11-1.
- **No topics** (F-P11-2), **no homepage** (F-P11-3), social preview = default
  auto-card (F-P11-4).
- Community health **42%**: README + license present; CONTRIBUTING, code of
  conduct, issue/PR templates absent (F-P11-5 — exactly the Phase 10
  P5-predicted gap, now measured on the public surface).
- Repository name is `GroundTruth`; package, docs, and prose use
  "Groundtruth" (F-P11-6, cosmetic).

## 4. Mobile rendering (390×844 viewport)

README prose reflows cleanly; the six-lens table renders narrow but readable;
sticky README/license tab bar works. Wide artifacts (benchmark table, ASCII
architecture diagram) fall back to horizontal scroll inside their blocks —
GitHub's standard behavior, acceptable. No broken rendering observed.

Screenshots (desktop + mobile) captured at baseline; retained outside the
repository (no binary-asset convention exists in-tree; findings above are the
durable record).

## 5. Baseline friction inventory (frozen; fixes belong to later phases)

| ID | Finding | Severity |
|---|---|---|
| F-P11-1 | About description shows literal `*asterisks*` | low, high-visibility |
| F-P11-2 | No repository topics — search discoverability zero | medium |
| F-P11-3 | No homepage URL | low |
| F-P11-4 | Default social preview card | low |
| F-P11-5 | Community profile 42% (no CONTRIBUTING/CoC/templates) | medium |
| F-P11-6 | Name casing `GroundTruth` vs "Groundtruth" | cosmetic |
| — | 0 tags / 0 releases on a v0.8 platform | medium (planned: release wave) |

## 6. Verdict

The engineering pipeline survived first contact with the public environment
with zero deviations: CI green on first run, byte-determinism held across
OS/architecture, public clone reproduces everything. Every baseline friction
finding is presentational, none functional. Phase A (public launch) of v1
Stabilization is complete; the post-baseline wave (releases, tags, CHANGELOG,
community files, topics, description text) may now proceed against this
frozen record.
