# Technical debt register (schema v1, policed by RC7 — Constitution Law 8).
# Migrated from SPEC.md §7 on 2026-07-18 (tribunal R4: SPEC keeps a pointer,
# never a mirror). State corrections made during migration are the three
# pre-registered findings of the v0.8 stewardship protocol:
#   #13 resolved in v0.4 but never flipped DONE in the prose table;
#   #11 said "stateful re-bench pending" while six stateful scorecards sat in runs/;
#   #12 timing said "v0.4" and is still open at v0.8.
# No numeric scoring fields — findings, never scores.
debt_schema: 1
items:
  - {id: 1, title: "Repo has no CI running its own tests", category: infrastructure, state: resolved, origin: "v0.1", evidence: [".github/workflows/ci.yml"], resolution: ".github/workflows/ci.yml"}
  - {id: 2, title: "LICENSE file missing (README claims MIT)", category: documentation, state: resolved, origin: "v0.1", evidence: ["LICENSE"], resolution: "LICENSE"}
  - {id: 3, title: "CLI scenario/validation paths were cwd-dependent", category: interfaces, state: resolved, origin: "v0.1", evidence: ["groundtruth/cli.py"], resolution: "groundtruth/cli.py"}
  - {id: 4, title: "Validation loader: a label typo silently minted a new category", category: validation, state: resolved, origin: "v0.2", evidence: ["groundtruth/core/validation.py"], resolution: "groundtruth/core/validation.py"}
  - {id: 5, title: "demo_agents.py sat at package root instead of products/agentprobe/", category: architecture, state: resolved, origin: "v0.1", evidence: ["groundtruth/products/agentprobe/demo_agents.py"], resolution: "groundtruth/products/agentprobe/demo_agents.py"}
  - {id: 6, title: "No schema_version on persisted Trace/Scorecard JSON", category: future-compat, state: resolved, origin: "v0.2", evidence: ["groundtruth/core/trace.py", "groundtruth/core/scorecard.py"], resolution: "groundtruth/core/trace.py"}
  - {id: 7, title: "Case.suite populated from YAML key product — naming entropy", category: naming, state: open, origin: "v0.1", evidence: ["groundtruth/core/dataset.py"], resolution: ""}
  - {id: 8, title: "robustness now blends safety + utility semantics; reconsider at v1.0", category: naming, state: open, origin: "v0.2", evidence: ["groundtruth/core/scorecard.py"], resolution: ""}
  - {id: 9, title: "Span kind is a free string (no enum/validation); fold into next schema_version bump", category: interfaces, state: open, origin: "v0.1", evidence: ["groundtruth/core/trace.py"], resolution: ""}
  - {id: 10, title: "MAX_STEPS was global, not per-case; measured — truncation is not a stall cause (9/9 stalls persist at 24 steps)", category: interfaces, state: resolved, origin: "v0.2", evidence: ["groundtruth/products/agentprobe/runner.py", "experiments/stall_confounds/REPORT.md"], resolution: "groundtruth/products/agentprobe/runner.py"}
  - {id: 11, title: "OllamaAgent default stays stateless (published v0.3 numbers depend on it); stateful variant built, measured, and re-benched in v0.4 — migration corrects the stale re-bench-pending note", category: architecture, state: accepted, origin: "v0.2", evidence: ["groundtruth/adapters/ollama_agent.py", "runs/scorecard-llama3.1-8b-stateful.json"], resolution: ""}
  - {id: 12, title: "No lint config / py.typed marker — timing drifted (declared v0.4, still open at v0.8); due pre-OSS release", category: dev-experience, state: open, origin: "v0.2", evidence: ["pyproject.toml"], resolution: ""}
  - {id: 13, title: "Loop-stall taxonomy gap — resolved by the non_completion category in v0.4; prose table never flipped DONE (migration correction)", category: taxonomy, state: resolved, origin: "v0.3", evidence: ["groundtruth/products/agentprobe/detectors.py", "tests/test_non_completion.py"], resolution: "tests/test_non_completion.py"}
  - {id: 14, title: "Manifest dimension D05 (dataset discrimination) is documented, not machine-derived from scorecards", category: meta, state: open, origin: "v0.6", evidence: ["runs/quality-manifest.json"], resolution: ""}
  - {id: 15, title: "Assurance report format has no schema_version (manifest does)", category: future-compat, state: open, origin: "v0.6", evidence: ["runs/assurance-report.md"], resolution: ""}
  - {id: 16, title: "README version parsed by regex convention; a format change fails CT6 loudly rather than silently — acceptable failure mode", category: interfaces, state: accepted, origin: "v0.3", evidence: ["groundtruth/meta/loader.py"], resolution: ""}
  - {id: 17, title: "CI regenerated both quality manifests but never diffed them against the committed copies — a stale committed manifest passed CI; closed by the CI diff step (v0.8)", category: meta, state: resolved, origin: "v0.6", evidence: [".github/workflows/ci.yml", "runs/quality-manifest.json"], resolution: ".github/workflows/ci.yml"}
